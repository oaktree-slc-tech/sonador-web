--extra-index-url https://code.oak-tree.tech/api/v4/projects/5/packages/pypi/simple

# Configuration Management
configobj

# Web Application Requirements
Django>=5.2,<5.3
django-cors-headers>=4.9.0
django-storages>=1.14.6
fastapi[standard]==0.115.12
pymemcache
blake3

# Core Dependencies
# nodeenv 1.3.5 imports the stdlib `pipes` module, removed in Python 3.13; the
# Ubuntu 26.04 base ships Python 3.14, so bump to a release that dropped it.
nodeenv>=1.9.1
# pydantic 2.12 is the first release whose pydantic-core ships Python 3.14
# wheels (required by the Ubuntu 26.04 base image); capped below 3.0.
pydantic>=2.12,<3


# Oak-Tree Libraries
acorn-base>=0.3
acorn-secure>=0.2.1
acorn-content>=0.2a1
acorn-microservices>=0.1.1
acorn-sassycss>=0.1.1
acorn-wgtauth>=0.2.1
acorn-largefiles>=0.1.1


# Google/S3 Cloud Storage
botocore==1.32.0
boto3==1.29.0
# The google-* libraries below were bumped off their 2021 pins: the old
# googleapis-common-protos/google-api-core shipped protobuf code generated with
# protoc < 3.19, which raises "Descriptors cannot be created directly" against a
# modern protobuf. These floors resolve cleanly on Python 3.14 (Ubuntu 26.04) and
# keep google-cloud-storage on the 2.x line supported by django-storages 1.14.6.
google-api-core>=2.17
google-auth>=2.29
google-cloud-core>=2.4
google-cloud-storage>=2.14,<3
google-crc32c>=1.5
google-resumable-media>=2.7
googleapis-common-protos>=1.63

# Visionaire Dependencies
tabulate>=0.9.0
